import React from 'react'

export default function page() {
  return (
    <div>page</div>
    /* 
      reactjs
      cơ chế render dữ liệu Reactjs
      nextjs
      hỗ trợ vừa render dữ liệu ở phía client và phía server
      NEXTJS:
      1. server component : mặc định khi các e tạo trang!
      2. client component : dùng "use client" - khi nào nên dùng: 
      cần tương tác dữ liệu dùng (use State, useEffect, các sự kiện(onClick,onChange..))

      Cách render dữ liệu ở trong NEXTJS
      I-render dữ liệu ở phía server
    */
  )
}
